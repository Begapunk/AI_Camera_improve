# --- Web 服务层（版本与当前开发环境一致，锁定防依赖漂移）---
flask==3.1.2
flask-cors==6.0.1
requests==2.32.5
pymysql==1.1.2
werkzeug==3.1.3
python-dotenv==1.2.2
itsdangerous==2.2.0
baidu-aip==4.16.13
openai==2.31.0
volcengine==1.0.207
numpy
# opencv-contrib-python 含本地人脸识别 cv2.face 子模块
# 注意：若已安装 opencv-python，需先 pip uninstall opencv-python 再安装此包
opencv-contrib-python

# --- ML 栈（与硬件/CUDA 强相关，不锁死；本机 torch 装的是 cu121 本地轮子）---
# --- 专业模式 PaliGemma 依赖 (首次安装较大，约 5GB GPU 显存) ---
torch
transformers>=4.47.0
accelerate
Pillow
# --- 骨骼追踪：YOLOv8n-pose（首次运行自动下载 6MB 模型到 ~/.ultralytics 缓存）---
ultralytics
# --- 手势识别：MediaPipe 官方 Gesture Recognizer（内置 Victory/剪刀手分类，无需自训练）---
mediapipe
